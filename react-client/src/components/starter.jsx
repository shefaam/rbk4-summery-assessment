import React from 'react';

const Starter = (props) => (
  <div>
  there is a proplem i couldnt solve it in the map step it can not define the items
    <h1> Pokemom! </h1>
  <h2> {props.name}</h2>
  </div>
);

export default Starter;
